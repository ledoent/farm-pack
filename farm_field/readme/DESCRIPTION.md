Fields / plots where crops grow. Each field has a name, code, acres (manual entry
for the SMB tier — `farm_field_geo` adds polygon-based compute), current crop,
crop year, organic-certified flag, and free-form notes.

Used as the location reference for `farm_planting` (what's planted where) and
`farm_harvest` (what's harvested where).
