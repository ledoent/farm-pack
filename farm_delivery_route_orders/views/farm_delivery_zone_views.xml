<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="farm_delivery_zone_view_list" model="ir.ui.view">
        <field name="name">farm.delivery.zone.list</field>
        <field name="model">farm.delivery.zone</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle" />
                <field name="name" />
                <field name="code" />
                <field name="color" widget="color_picker" />
                <field name="active" optional="hide" />
            </list>
        </field>
    </record>

    <record id="farm_delivery_zone_view_form" model="ir.ui.view">
        <field name="name">farm.delivery.zone.form</field>
        <field name="model">farm.delivery.zone</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name" />
                            <field name="code" />
                            <field name="sequence" />
                        </group>
                        <group>
                            <field name="color" widget="color_picker" />
                            <field name="active" />
                            <field
                                name="company_id"
                                groups="base.group_multi_company"
                            />
                        </group>
                    </group>
                    <group>
                        <field name="description" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="farm_delivery_zone_action" model="ir.actions.act_window">
        <field name="name">Delivery Zones</field>
        <field name="res_model">farm.delivery.zone</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
