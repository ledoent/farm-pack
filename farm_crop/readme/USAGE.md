Browse the seeded catalog at **Farm → Configuration → Crops**. To add a custom
crop: hit *New*, fill name + category (vegetable, fruit, grain, etc.), optionally
latin name + botanical family (helps with rotation planning) + days to maturity.

Crops referenced from `farm.field`, `farm.planting`, and `farm.harvest`. Archive
unused entries via the Active toggle rather than deleting.
